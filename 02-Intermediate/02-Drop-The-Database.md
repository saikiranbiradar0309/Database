# Drop the Database

You can delete the entire database and all its tables using the `DROP DATABASE` statement:

```sql
DROP DATABASE <database_name>;
```

**Example:**

```sql
DROP DATABASE selenium;
```

> [!WARNING]
> This will permanently delete everything in that database, including all tables and data!